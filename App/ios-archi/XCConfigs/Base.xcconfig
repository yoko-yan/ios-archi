PRODUCT_BUNDLE_IDENTIFIER = com.sample
PRODUCT_BUNDLE_IDENTIFIER_TESTS = $(PRODUCT_BUNDLE_IDENTIFIER)Tests
PRODUCT_BUNDLE_IDENTIFIER_UITESTS = $(PRODUCT_BUNDLE_IDENTIFIER)UITests

// Development Team ID (個人情報なのでLocal.xconfigで上書き推奨)
DEVELOPMENT_TEAM =

#include? "Local.xcconfig"
