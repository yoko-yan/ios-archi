---
description: 実装後の確認サイクル（ビルド→テスト→レビュー→動作確認）(project)
allowed-tools: Bash, Read, Edit, Write, Glob, Grep, mcp__mobile-mcp
---

> 一通り実装が終わったら、このコマンドを実行してください。

## サイクル

`build` → `run-tests` → `self-review` → `verify-app` → 完了報告
（問題あれば修正して繰り返し）

## 各フェーズ

### 1. ビルド確認
`build` サブエージェントを実行。エラーがあれば修正。

### 2. テスト
`run-tests` サブエージェントを実行。失敗があれば修正。

### 3. レビュー
`self-review` サブエージェントを実行。問題があれば修正してPhase 1に戻る。

### 4. 動作確認（UI変更時のみ）
`verify-app` サブエージェントを実行してスクリーンショット確認。

### 5. 完了報告
変更ファイル、品質確認結果を報告。

## エラー時
- 3回試行しても解消しない場合はユーザーに相談
